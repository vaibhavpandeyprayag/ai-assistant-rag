# Runtime dependencies (Phase 1 subset; extended in later phases).
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32
pypdf==6.16.1
python-docx==1.2.0
langchain-text-splitters==1.1.2
# Torch + sentence-transformers are Windows-only (install markers): the CUDA
# wheels are ~2.5 GB and do not fit Render's free-tier build. Render uses
# EMBEDDING_PROVIDER=hf (Hugging Face Inference API) instead of local
# embeddings, so these are unnecessary there.
torch==2.13.0 ; sys_platform == "win32"
sentence-transformers==6.0.0 ; sys_platform == "win32"
huggingface-hub==1.28.0
chromadb==1.5.9
httpx==0.28.1
